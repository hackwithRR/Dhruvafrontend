# Firestore index errors (notes)

This file collects missing composite index links generated by Firestore when a query fails with:
`failed-precondition: The query requires an index.`

## Current error (createComposite link from console)
- Query involves collection group/collection with chained `where(...)` + `orderBy(...)`.
- Create the composite index using the link shown in the error toast/log.

